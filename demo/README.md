# Demo vault

Sample notes for screenshots and for trying the plugin: a household of four,
eleven recipes, the products they use, two shops and a planned week.

`scripts/screenshots.mjs` copies this folder into a throwaway vault, installs
the plugin build and captures the screenshots in `docs/screenshots/`.
